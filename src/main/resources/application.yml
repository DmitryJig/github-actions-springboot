server:
  port: 8189
  servlet:
    context-path: /app
  error:
    include-message: always

spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: validate

  datasource:
    url: jdbc:postgresql://172.24.0.11:5432/shop_db
    username: adminuser
    password: 747482hfsjbc[
    driverClassName: org.postgresql.Driver